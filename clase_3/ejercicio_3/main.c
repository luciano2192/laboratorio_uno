#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;
    char nombres[15];
    char apellido[11];

    printf("Nombres: ");
    fgets( nombre , 15 , stdin );

    printf("Apellido: ");
    fgets( apellido , 11 , stdin );

    strlwr( nombres );
    strlwr( apellido );
    for( i = 0 ; i < 15 ; i++ ) {

    }

    return 0;
}
